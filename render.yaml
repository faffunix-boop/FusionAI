services:
  - type: web
    name: nexa-chatbot
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GEMINI_KEY
        sync: false
      - key: GROQ_KEY
        sync: false
